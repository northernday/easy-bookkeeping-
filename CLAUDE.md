# 项目规则

## 会话启动

每次新对话开始时，按顺序调用以下 Skill：

1. **using-superpowers** — 确保所有后续操作都经过 Skill 检查
2. **karpathy-guidelines** — 编码时不过度设计、精准改动、先想再做
3. **caveman** — 输出 Token 压缩 65%，full 模式
4. **simplify** — 代码完成后审查可复用性、简洁性和效率

这些是元技能，只加载行为规则而非数据，不会占用实质上下文。
## 技术栈
- Electron + React + TypeScript
- Zustand 状态管理
- JSON 文件存储（零依赖）
- xlsx 库处理 Excel

## 关键约定
- 所有文件操作必须在主进程，渲染进程通过 IPC
- 分类/账户 ID 是租户隔离的（跨租户漂移会导致统计归零）
- 导入流程：Parse → Preview → Commit 三步模式
- 存储用软删除 + deleted_at 标记

## 不做什么
- 不要引入新依赖，优先手写！
- 不要过度设计，YAGNI

## 获取资源
- 需要外部资源时，先从 GitHub 搜索，列出候选方案及理由，由我决策
- 确认后下载到 D 盘（除非功能要求必须在项目目录）
- 评估安全性后再推荐

## 调试代码
- 当一个bug反复调试，消耗token>1000w时，将解决方法写入memory模块
- 当一个bug反复调试过多次时，先做依赖性分析，看能否通过删除这个功能来绕过这个bug（需要请示我）
- 同一方向尝试 3 次无果后，主动提出替代方案而非继续硬刚
## 命令
- `npm run dev` — 启动开发环境
- `npm run build` — 生产构建
- `npm test` — 运行测试
## 代码调试
- 记录每一步的思路，每一步修改务必留痕